import styled from "@emotion/styled"

const Wrapper = styled.div`
    height: 50px;
    background-color: green;
`

const LayoutFooter = () => {
    return <Wrapper>Footer 영역</Wrapper>
}

export default LayoutFooter