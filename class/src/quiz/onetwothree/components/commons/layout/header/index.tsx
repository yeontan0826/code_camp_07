import styled from "@emotion/styled"

const Wrapper = styled.div`
    height: 50px;
    background-color: red;
`

const LayoutHeader = () => {
    return <Wrapper>Header 영역</Wrapper>
}

export default LayoutHeader